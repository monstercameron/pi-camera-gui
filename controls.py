def cameraControls(pygame, event, menuPos, settings):
        if event.type == pygame.KEYDOWN:
            # print(event.key)
            if event.key == pygame.K_UP:
                print("up") 
            elif event.key == pygame.K_DOWN:
                print("down") 



def menuNavigator(menuPosArr, menus, settings):
    pass